spring:
  application:
    name: xinnet-api
  cloud:
    config:
      discovery:
        enabled: true
        service-id: config-server
server:
  port: 5678
eureka:
  client:
    service-url:
      defaultZone: http://admin:passwd@127.0.0.1:7777/eureka